dependencies:
  post:
    - pip install -r requirements.txt

test:
  override:
    - mkdir $HOME/docs/build
    - sphinx-build -b $HOME/docs -c $HOME/docs/conf.py $HOME/docs/build

  post:
    - cp -r $HOME/docs/build $CIRCLE_ARTIFACTS
